server:
  port: 9300
  
spring:
  application:
    name: TransactionMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        defaultContext: application
        data-key: data
        profileSeparator: '::'
        format: YAML
        
      

     
