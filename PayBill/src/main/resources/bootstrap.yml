server:
  port: 9600
  
spring:
  application:
    name: PayBillMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        defaultContext: application
        data-key: data
        profileSeparator: '::'
        format: YAML
        
      

     
